


// Instantiate the module
M_REG instance_name (
    .clk(clk), 
    .reset(reset), 
    .WE(WE), 
    .instr_in(instr_in), 
    .pc_in(pc_in), 
    .ALU_in(ALU_in), 
    .rt_in(rt_in), 
    .EXT_in(EXT_in), 
    .instr_out(instr_out), 
    .pc_out(pc_out), 
    .ALU_out(ALU_out), 
    .rt_out(rt_out), 
    .EXT_out(EXT_out)
    );


