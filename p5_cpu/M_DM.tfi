


// Instantiate the module
M_DM instance_name (
    .pc(pc), 
    .clk(clk), 
    .reset(reset), 
    .DMaddr(DMaddr), 
    .DMdata(DMdata), 
    .WE(WE), 
    .DMtype(DMtype), 
    .DMout(DMout)
    );


